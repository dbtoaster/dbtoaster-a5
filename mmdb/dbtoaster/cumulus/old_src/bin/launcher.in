#!/bin/bash

SRCDIR=`dirname $0`/../src
LIBDIR=`dirname $0`/../lib
INCLUDES="
  -I $LIBDIR
  -I $LIBDIR/thrift/rb
  -I $SRCDIR/bin
  -I $SRCDIR/common
  -I $SRCDIR/node
  -I $SRCDIR/switch
  -I $SRCDIR/slicer
  -I $SRCDIR/toaster
  -I $SRCDIR/query
  -I $SRCDIR/conf
  -I $SRCDIR/gen-rb"

#RUBY_EXE=ruby
RUBY_EXE="$LIBDIR/jruby-1.4.0/bin/jruby -w"

#echo ========================
#echo $RUBY_EXE $INCLUDES $SRCDIR/bin/--SCRIPT-- $*
#echo ========================
$RUBY_EXE $INCLUDES $SRCDIR/bin/--SCRIPT-- $*
