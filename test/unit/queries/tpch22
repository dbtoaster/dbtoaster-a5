{
  :path => "test/queries/tpch/query22.sql",
  :datasets => {
    "standard" => {
      :toplevels => {
        "QUERY22" => {
          :type => :onelevel,
          :expected => results_file("test/results/tpch/query22.csv"),
        }
      }
    },
    
    "tiny" => {
      :subs => [ [/schemas\.sql/, "schemas_tiny.sql"] ],
      :toplevels => {
        "QUERY22" => {
          :type => :onelevel,
          :expected => { }
        }
      }
    },

    "big" => {
      :subs => [ [/schemas\.sql/, "schemas_big.sql"] ],
      :toplevels => {
        "QUERY22" => {
          :type => :onelevel,
          :expected => results_file("test/results/tpch/query22_big.csv")
        }
      }
    },
    
    "standard_del" => {
      :subs => [ [/schemas\.sql/, "schemas_del.sql"] ],
      :toplevels => {
        "QUERY22" => {
          :type => :onelevel,
          :expected => results_file("test/results/tpch/query22_del.csv")
        }
      }
    },
    
    "tiny_del" => {
      :subs => [ [/schemas\.sql/, "schemas_tiny_del.sql"] ],
      :toplevels => {
        "QUERY22" => {
          :type => :onelevel,
          :expected => results_file("test/results/tpch/query22_tiny_del.csv")
        }
      }
    },

    "big_del" => {
      :subs => [ [/schemas\.sql/, "schemas_big_del.sql"] ],
      :toplevels => {
        "QUERY22" => {
          :type => :onelevel,
          :expected => results_file("test/results/tpch/query22_big_del.csv")
        }
      }
    }
    
  }
}
